{
  "name": "lakshmipur-teacher-phonebook",
  "private": true,
  "version": "1.0.0",
  "description": "Lakshmipur Government College Teacher Phone Book (mobile web app + Android wrapper)",
  "scripts": {
    "data": "python tools/generate_phonebook_data.py",
    "serve": "cd mobile && python -m http.server 8000",
    "cap:sync": "npx cap sync",
    "sync:android": "npm run data && npx cap sync android",
    "android": "npx cap open android",
    "apk:debug": "npm run sync:android && cd android && gradlew assembleDebug",
    "apk:release": "npm run sync:android && cd android && gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
